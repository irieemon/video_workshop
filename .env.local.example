# Supabase
NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# OpenAI
OPENAI_API_KEY=sk-your-api-key

# Note: The same OPENAI_API_KEY is used for:
# - AI agent roundtable (GPT models)
# - Sora video generation API

# AI Model Configuration
# SINGLE VARIABLE controls ALL AI operations (agents, synthesis, vision)
# Change this ONE line to switch models everywhere
OPENAI_MODEL=gpt-5-chat-latest

# Supported models:
# - gpt-5-chat-latest (recommended, latest GPT-5)
# - gpt-4o (stable fallback)
# - gpt-4o-mini (lightweight, faster, cheaper)

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Cron (for usage reset)
CRON_SECRET=your-random-secret
